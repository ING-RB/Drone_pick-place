%getNamespaceForPrefix Get the namespace that defines a prefix
%    url = getNamespaceForPrefix(resolver,prefix) returns the URL
%    of the namespace that defines the specified prefix. 
%    matlab.io.xml.xpath.PrefixResolver defines but does not
%    implement this method. You must provide an implementation in
%    a derived class.

%    Copyright 2020-2021 MathWorks, Inc.
%    Built-in function.
