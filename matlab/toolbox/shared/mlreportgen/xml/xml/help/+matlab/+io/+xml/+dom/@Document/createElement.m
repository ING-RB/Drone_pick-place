%createElement Create an element
%    elem = createElement(thisDoc,elemName) creates and returns an element
%    with the specified name. If the document has a document type and the
%    document type defines default attributes, this method adds the
%    default attributes to the element that it creates.
%
%    See also matlab.io.xml.dom.Element, 
%    matlab.io.xml.dom.Document.createElementNS

%    Copyright 2020 MathWorks, Inc.
%    Built-in function.