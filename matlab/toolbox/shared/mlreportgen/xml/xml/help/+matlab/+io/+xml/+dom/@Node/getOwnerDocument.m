%getOwnerDocument Get the document that owns this node.
%    doc = getOwnerDocument(thisNode) returns the document that owns this
%    node. The owner document is the document that created or imported
%    this node.

%    Copyright 2020 MathWorks, Inc.
%    Built-in function.