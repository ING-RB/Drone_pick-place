%item Get a node list item specified by a zero-based index
%    node = item(thisNodeList,index) returns the item specified by index,
%    where 0 specifies the first item in the list, 1, the second item,
%    and so on.
%
%    matlab.io.xml.dom.NodeList.node

%    Copyright 2020 MathWorks, Inc.
%    Built-in function.