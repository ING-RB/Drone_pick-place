%setTextContent Set the text content of a document (noop)
%    setTextContent(thisDoc,textContent) has no effect because a 
%    document cannot directly contain a text node.
%
%    Note: this method is provided for backward compatibility with 
%    existing MATLAB code originally based on the Java API for XML
%    Processing (JAXP).

%    Copyright 2020 MathWorks, Inc.
%    Built-in function.