%node Get a node map item specified by a one-based index
%    node = item(thisNodeMap,index) returns the item specified by index,
%    where 1 specifies the first item in the list, 2, the second item,
%    and so on.

%    Copyright 2021 MathWorks, Inc.
%    Built-in function.