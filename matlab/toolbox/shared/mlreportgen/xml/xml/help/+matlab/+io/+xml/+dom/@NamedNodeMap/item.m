%item Get a node map item specified by a zero-based index
%    node = item(thisNodeMap,index) returns the item specified by index,
%    where 0 specifies the first item in the list, 1, the second item,
%    and so on.

%    Copyright 2020 MathWorks, Inc.
%    Built-in function.