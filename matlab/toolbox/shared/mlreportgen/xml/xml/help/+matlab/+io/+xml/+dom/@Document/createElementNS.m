%createElementNS Create an element in a namespace
%    elem = createElementNS(thisDoc,namespaceURI,qualifiedName) creates and 
%    returns an element in the specified namespace with the specified
%    qualified name. If the document has a document type and the
%    document type defines default attributes, this method adds the
%    default attributes to the element that it creates.
%
%    See also matlab.io.xml.dom.Element,
%    matlab.io.xml.dom.Document.createElement

%    Copyright 2020 MathWorks, Inc.
%    Built-in function.