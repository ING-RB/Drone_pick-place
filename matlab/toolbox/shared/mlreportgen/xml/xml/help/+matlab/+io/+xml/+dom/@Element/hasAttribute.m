%hasAttribute Check if element has attribute with name
%    tf = hasAttribute(thisElem,name) returns true if this element has an
%    attribute with the specified name; otherwise, false.
%
%    See also matlab.io.xml.dom.Element.hasAttributes, 
%    matlab.io.xml.dom.Element.hasAttributeNS  

%    Copyright 2020 MathWorks, Inc.
%    Built-in function.