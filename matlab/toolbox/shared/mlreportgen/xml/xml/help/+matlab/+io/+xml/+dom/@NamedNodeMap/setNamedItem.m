%setNamedItem Add a named node to a named node map
%    node = setNamedItem(thisNodeMap,node) adds the named node to
%    thisNodeMap. This method also adds the node to the node from which the
%    map was created.

%    Copyright 2021 MathWorks, Inc.
%    Built-in function.