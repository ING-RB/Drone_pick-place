%getTarget Get this processing instruction's target
%    target = getTarget(thisPI) returns this processing instruction's
%    target as a character vector.

%    Copyright 2020 MathWorks, Inc.
%    Built-in function.