%normalize Normalize text content of this element
%    normalize(thisElem) removes empty text nodes from this element and
%    combines adjacent text nodes ina a single text node. This ensures 
%    that the element has the same structure that it would have after
%    saving and reloading the document in which it resides.
%
%    See also matlab.io.xml.dom.Document.normalizeDocument

%    Copyright 2020 MathWorks, Inc.
%    Built-in function.