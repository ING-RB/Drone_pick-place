%getInternalSubset Get entities and notations defined by document
%    subset = getInternalSubset(thisDoctype) returns a character vector
%    containing the document type's internal subset. The internal subset
%    comprises the entities and notations defined in the markup from which
%    this document type was parsed.

%    Copyright 2020 MathWorks, Inc.
%    Built-in function.