%createNSResolver Create a namespace resolver for XPath expressions
%    resolver = createNSResolver(thisDoc,resolverNode) creates a 
%    namespace resolver that uses the specified node to find the namespace
%    for the prefix of a qualified element name that occurs in an XPath
%    expression.
%
%    See also matlab.io.xml.dom.Document.evaluate,
%    matlab.io.xml.dom.XPathNSResolver

%    Copyright 2020 MathWorks, Inc.
%    Built-in function.