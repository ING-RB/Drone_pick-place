%getStyle Find a style or styles in this stylesheet by name
%    styles = getStyle(this,styleName) returns styles whose names match
%    styleName, specified as a string scalar or character vector. If no
%    styles have the specified name, this method returns an empty double.
% 
%    styles = getStyle(this,styleName,type) returns styles of the specified
%    type whose names match styleName. If no styles have the specified type
%    and name, this method returns an empty double. The type argument can
%    be specified as any of the following values: 
%
%         * text
%         * paragraph
%         * linked
%         * table
%         * list
%         * html
%         * pdf
%         * docx

%    Copyright 2023 MathWorks, Inc.
%    Built-in function.
