%append Append text and images to this reference
%    textObj = append(obj, 'text') appends a Text object constructed
%    from the specified text string to the document reference.
%
%    textObj = append(obj, 'text', 'styleName') appends a Text object 
%    constructed from the specified text string and having the specified
%    style name to the document reference.
%
%    domObj = append(obj, domObj) appends a DOM object to the document
%    reference. The object can be any of the following types:
%
%        * Image
%        * Text
%        * CustomElement

%    Copyright 2019 MathWorks, Inc.
%    Built-in function.
