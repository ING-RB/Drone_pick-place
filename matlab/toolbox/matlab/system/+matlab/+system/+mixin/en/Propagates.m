classdef Propagates< handle
%matlab.system.mixin.Propagates is an obsolete mixin class.
%   The Propgates mixin was a base class for System objects that defines
%   the System object's output size, data type, and complexity. This mixin
%   is combined into matlab.System and is no longer required.

 
%   Copyright 2012-2020 The MathWorks, Inc.

    methods
        function out=Propagates
        end

    end
end
