function n = dotListLength(obj,indexOp,indexContext)

%

%   Copyright 2024 The MathWorks, Inc.

    % The length of the outputs is 1 because the output is configured to be
    % a cell array
    n = 1;
end
