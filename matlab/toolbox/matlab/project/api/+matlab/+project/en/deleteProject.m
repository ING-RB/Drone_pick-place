%deleteProject Remove project from disk, while keeping the managed files
%    matlab.project.deleteProject(projectLocation) removes a
%    project, so the specified folder is no longer managed by a
%    project. This function only removes project definition files.
%
%    This method fails if the project in projectLocation is currently loaded.

%  Copyright 2019-2022 The MathWorks, Inc.
