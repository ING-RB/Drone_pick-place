classdef (Abstract) CodeGenSection < handle
    % CodeGenSection Obtains metadata on MATLAB classes or functions which can be used
    % to generate C++ code that nicely wraps said classes or functions

%   Copyright 2020-2023 The MathWorks, Inc.
    
    properties (Abstract = true, Access = public)
        SectionName (1,1) string
        SectionContent (1,1) string  % cpp code to be generated by section
        SectionMetaData
        IndentLevel % The section starts with this many indents
    end
    
    methods (Abstract = true)
        read(obj) % Populate content objects by reading metadata
    end
    
end
