<?xml version="1.0"?>
<!-- Copyright 2023-2024 The MathWorks, Inc. -->
<rsccat version="1.0" locale="en_US" product="MATLABTest">
  <message>
      <entry key="UnsupportedBaselineFile">Unable to load baseline data from {0} because the file type is not supported.</entry>

      <!-- Argument validation -->
      <entry key="MustBeBaseline">Expected value must be a baseline object.</entry>

      <!-- UI Messages -->
      <entry key="Confirm">Yes</entry>
      <entry key="Cancel">Cancel</entry>

      <!-- Baseline creation -->
      <entry key="CreateBaseline">Create baseline from recorded test data</entry>
      <entry key="CreateTitle">Creation Complete</entry>
      <entry key="CreatedBaseline">Created baseline data and saved it to:</entry>
      <entry key="CreateFailedTitle">Baseline Creation Failed</entry>
      <entry key="CreateFailed">Failed to create a new baseline.</entry>

      <!-- Baseline update-->
      <entry key="UpdateBaseline">Update baseline from recorded test data</entry>
      <entry key="UpdateTitle">Update Baseline</entry>
      <entry key="UpdatePrompt">Are you sure you want to overwrite the baseline with new data?</entry>
      <entry key="UpdateFailedTitle">Baseline Update Failed</entry>
      <entry key="UpdateFailed">Failed to update the baseline using the recorded test data.</entry>

      <!-- Baseline data export -->
      <entry key="ExportBaseline">Export test data to workspace</entry>
      <entry key="ExportedBaseline">Exported the baseline data (expected value) to the workspace as "{0}".</entry>
      <entry key="ExportedActual">Exported the actual value to the workspace as "{0}".</entry>
      <entry key="ExportCompleteTitle">Export Complete</entry>
      <entry key="ExportFailedTitle">Export Failed</entry>
      <entry key="ExportFailed">Failed to export test data to workspace.</entry>
  </message>
</rsccat>
