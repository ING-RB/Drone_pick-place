<?xml version="1.0" encoding="UTF-8" ?>
<!-- Copyright 2019-2021 The MathWorks, Inc. -->
<rsccat version="1.0"
        locale="en_US"
        product="SimulinkExecution"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:noNamespaceSchemaLocation="../../schema/.xsd">

  <message>
    <entry key="EnumLoadingNotSupportedInRaccel">
    Unable to load 'Enum Type' data for 'values' field at index ({0,number,integer}) in the initial state. Enum data type is not supported for loading initial states.
    </entry>

    <entry key="SignalCountMismatch">
        There are {2, number, integer} states in the model ''{0}'' while {1, number, integer} states were provided as initial state values.
    </entry>

    <entry key="InsufficientSignalCount">
        There are {2, number, integer} states in the model ''{0}'' while only {1, number, integer} states were provided as initial state values. Specify initial conditions for all states in Rapid accelerator mode.
    </entry>

    <entry key="BlockStateNameMissing">
        Block/State ''{0} {1}'' present in the initial state is missing in the model ''{2}''.
    </entry>

    <entry key="LabelMismatch">
        Label of signal {0, number, integer} in initial state is {1}, whereas the model has the label {2}.
    </entry>

    <entry key="StateNameMismatch">
        State name of signal {0, number, integer} in initial state is {1}, whereas the model has the state name {2}.
    </entry>

    <entry key="InvFieldInInitStateStruct">
        Unable to load the specified initial state for model ''{0}''. The initial state must be a structure containing a {1} structure array. The {2} structure should have the following fields: ''{3}'', ''{4}'', and ''{5}''
    </entry>

    <entry key="InvBlockNameInInitStateStruct" note="stringDatatypeUpdate">
        Unable to load the specified initial state for model ''{0}''. The {1}({2,number,integer}).{3} field in the initial state structure must be a character vector specifying the path to a block in the model.
    </entry>

    <entry key="InvLabelInInitStateStruct" note="stringDatatypeUpdate">
        Unable to load the specified initial state for model ''{0}''. The {1}({2,number,integer}).{3} field in the initial state structure must be a character vector specifying a valid state label
    </entry>

    <entry key="InvMdlRefFieldInInitStateStruct">
        Unable to load the specified initial state for model ''{0}''. The {1}({2,number,integer}).{3} field in the initial state structure must be a logical scalar value
    </entry>

    <entry key="MdlRefFieldMismatch">
        Value mismatch in the initial state for model ''{0}''. The {1}({2,number,integer}).{3} field in the initial state structure is not matching with the value stored in the model
    </entry>

    <entry key="InvValuesComplexityInInitStateStruct">
        Type mismatch in the initial state for model ''{0}''. The {1}({2,number,integer}).{3} field in the initial state structure is complex valued, whereas the corresponding state ''{4}'' in block ''{5}'' is not complex valued
    </entry>

    <entry key="InvValuesDataTypeInInitStateStruct">
        Data type mismatch in the initial state for model ''{0}''. The {1}({2,number,integer}).{3} field in the initial state structure is of type {4}, whereas the corresponding state ''{5}'' in block ''{6}'' is of type {7}
    </entry>

    <entry key="InvValuesInInitStateStruct">
        Unable to load the specified initial state for model ''{0}''. The {1}({2,number,integer}).{3} field in the initial state structure must be a finite numeric array
    </entry>

    <entry key="InvValuesSizeInInitStateStruct">
        Size mismatch in the initial state for model ''{0}''. The {1}({2,number,integer}).{3} field in the initial state structure has {4,number,integer} element(s), whereas the corresponding state ''{5}'' in block ''{6}'' has {7,number,integer} element(s)
    </entry>

    <entry key="DuplicateBlockName">
        Multiple states in the initial state structure have the same block name {0}. It could be due to bus expanded signals which are not supported with structure format.
    </entry>

    <entry key="InvFieldInInitStateDataset">
        Unable to load the specified initial state for model ''{0}''. The initial state  must be of 'Simulink.SimulationData.Dataset' type with an array of 'Simulink.SimulationData.State' type with following fields: ''{1}'', ''{2}'', ''{3}'' and ''{4}''
    </entry>

    <entry key="LabelMismatchDataset">
        Label of state ''{0}'' in initial state is ''{1}'', whereas the model has the label ''{2}''.
    </entry>

    <entry key="StateNameMismatchDataset">
        State name of state ''{0}'' in initial state is ''{1}'', whereas the model has the state name ''{2}''.

    </entry>
    <entry key="InvValuesInInitStateDataset">
        Unable to load the specified initial state for model ''{0}''. The values of BlockPath ''{1}'' in the initial state structure must be a finite numeric array
    </entry>

    <entry key="InvValuesSizeInInitStateDataset">
        Size mismatch in the initial state for model ''{0}''. The BlockPath ''{1}'' in the initial state structure has {2,number,integer} element(s), whereas the corresponding value in BlockPath ''{3}'' has {4,number,integer} element(s)
    </entry>

    <entry key="InvValuesDataTypeInInitStateDataset">
        Data type mismatch in the initial state for model ''{0}''.The value of ''{1}'' in the initial state structure is of type ''{2}'', whereas the corresponding state ''{3}'' in block ''{4}'' is of type ''{5}''
    </entry>

    <entry key="InvValuesComplexityInInitStateDataset">
        Type mismatch in the initial state for model ''{0}''. The ''{1}'' in the initial state structure is complex valued, whereas the corresponding state ''{2}'' in block ''{3}'' is not complex valued
    </entry>

    <entry key="NotLoggingFinalStatesRapidAccel">
        Rapid Accelerator simulation will not log Final states because, top model or the referenced models contain states with unsupported data types.
    </entry>

    <entry key="BlkIgnoringUsedAsDStateFlag">The discrete state of ''{0}'' will not be logged because its data type ''{1}'' is not built-in.</entry>

    <entry key="WarnAboutStatesNotLogged"> The model ''{0}'' is logging states,
    but the following referenced models contain a state that cannot be logged
    because its data type is not supported for logging: ''{1}''.  To see which
    states are not logged, simulate these referenced models and check for
    warnings. </entry>

<entry key="InternalErrorSaveFinalStateDataset"> Cannot save final states in Dataset Format in Rapid Accelerator simulation for model ''{0}''. Switch to normal mode for saving final states in Dataset Format.
    </entry>
</message>

</rsccat>
